# configs/deployment/

Deployment target definitions — which environment maps to which orchestrator target. Consumed by CI/CD (.github/workflows/), not by application code.
Spec: docs/06_project_structure.md §2.3, §7.3.
