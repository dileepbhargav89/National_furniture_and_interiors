// Barrel export — docs/06_project_structure.md §5 (@nfi/shared: types/, enums/, validation/,
// constants/). No cross-stack contracts exist yet (Sprint 1+); kept as a valid, empty module so
// tsc/vitest have a real entry point to check against Sprint 0's tooling.
export {};
