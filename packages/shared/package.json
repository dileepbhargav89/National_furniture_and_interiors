{
  "name": "@nfi/shared",
  "private": true,
  "version": "0.1.0",
  "description": "Cross-stack contract: types, enums, validation (Zod), constants. Depends on nothing else — consumed by @nfi/api-client, @nfi/database, apps/api, and both frontend apps. docs/05_repository_strategy.md §10, docs/06_project_structure.md §5.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@nfi/eslint-config": "workspace:*",
    "@nfi/tsconfig": "workspace:*",
    "eslint": "^9.13.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
