{
  "name": "@nfi/config",
  "private": true,
  "version": "0.1.0",
  "description": "Runtime-consumable shared frontend configuration: base Tailwind theme, shared Next.js config fragments. Distinct from root configs/ (non-code data). docs/06_project_structure.md §2.3, §5.",
  "main": "./src/index.ts",
  "scripts": {
    "lint": "eslint ."
  },
  "dependencies": {
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@nfi/eslint-config": "workspace:*",
    "@nfi/tsconfig": "workspace:*",
    "eslint": "^9.13.0"
  }
}
