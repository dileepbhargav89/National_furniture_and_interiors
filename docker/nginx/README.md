# docker/nginx/

Reverse proxy / TLS termination config. docs/06_project_structure.md §7.1, docs/10_devops_architecture.md §9 (Infrastructure — provider-specific detail lives there; this folder holds only the Nginx config artifact itself, if a self-managed reverse proxy is used).
