# assets/

Raw/source creative assets — never imported by running code, never served directly.
Spec: docs/06_project_structure.md §2.2, docs/04_architecture_decision.md §10.11.

- `brand/` — logo sources
- `design-system/` — design-system exports (feeds `@nfi/config`/`@nfi/ui`, not consumed directly)
- `email-templates/` — source templates for `notifications` module's email adapter
- `marketing/` — campaign/marketing creative sources
