# public/

Global static assets shared across apps, used sparingly — distributed into each app's own `apps/*/public/` at build time, not served from here directly.
Spec: docs/06_project_structure.md §2.2, docs/04_architecture_decision.md §10.8.
